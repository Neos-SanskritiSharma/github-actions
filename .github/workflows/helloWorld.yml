name: Create File

on:
  push:
    branches:
      - main

jobs:
  create-file:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
      
    - name: Create .github/workflows directory
      run: mkdir -p test

    - name: Create testing.json file
      run: echo "{\"statement\":\"Hello World\"}" > test/testing.json
