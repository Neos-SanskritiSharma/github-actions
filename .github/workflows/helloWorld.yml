name: hello-world
on: push
jobs:          
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: |
                get_extensions_apiCall="{\"statement\":\"Hello World\"}"
                echo $get_extensions_apiCall > .github/workflows/test.json
    
                               
