name: hello-world
'on': push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: |
             envid=260
             echo $envid
             echo https://api.boomi.com/api/rest/v1/EnvironmentExtensions/$'envId'
            
