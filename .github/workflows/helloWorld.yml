name: Create File
on:
  push:
    branches:
      - main
jobs:
  create_file:
    runs-on: ubuntu-latest
    steps:
     - name: Extract Filename
       run: |
         fileToRead=./boomi/DeploymentConfiguration/ExtensionsUpdateConfiguration/ProjectDetails.DPP.json
         arrIN=(${fileToRead//// })
         fileToRead=$(echo ${arrIN[5]})
         echo "Filename is: $fileToRead"
